import { FC } from "react";

interface SideBarProps {}

const SideBar: FC<SideBarProps> = ({}) => {
  return <div className="bg-gray-200 flex-col w-[20vw] h-[92vh]">SideBar</div>;
};

export default SideBar;
